= render 'all'

#selected_tags
  - for tag in @selected_tags
    .tag
      = tag.name

#tagged_items
  - for item in @tagged_items
    .item
      - photo_url = item.photos[0].file if item.photos.any?
      = image_tag photo_url, alt: nil
      .description
        = item.description