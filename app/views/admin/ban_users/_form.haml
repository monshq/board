= form_for @admin_comment, url: admin_user_ban_path(user_id: @user.id) do |f|
  = f.error_messages
  .field
    = f.hidden_field :action_type
  .field
    .label= f.label     :comment
    .input= f.text_area :comment
  .field
    .input= f.submit
